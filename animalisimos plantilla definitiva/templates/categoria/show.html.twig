{% extends 'base.html.twig' %}

{% block title %}Categoria{% endblock %}

{% block body %}
    {% include "navadmin/navadmin.html.twig" %}

    <div class="col-7 mt-4">
    <h2>Categoria</h2>
    <div class="row">
    <div class="col-7 mt-2 text-left">
    <table class="table  table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorium.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ categorium.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ categorium.descripcion }}</td>
            </tr>
              <tr>
                <th>Borrar</th>
                <td>{{ include('categoria/_delete_form.html.twig') }}</td>
            </tr>
            <tr>
                <th>Editar</th>
                <td><a href="{{ path('categoria_edit', {'id': categoria.id}) }}"><i class="far fa-edit"></i></a></td>
            </tr>
        </tbody>
    </table>

     <a href="{{ path('categoria_index') }}"><i class="fas fa-arrow-alt-circle-left"></i>Volver a Lista</a>
</div>
    </div>
    </div>
   
{% endblock %}
