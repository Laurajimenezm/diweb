

{% extends 'base.html.twig' %}

{% block title %}Gatoperro{% endblock %}

{% block body %}
{% include "navadmin/navadmin.html.twig" %}

<div class="col-7 mt-4">
    <h2>Tipo de Animal</h2>
    <div class="row">
    <div class="col-7 mt-2 text-left">
    <table class="table  table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gatoperro.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ gatoperro.nombre }}</td>
            </tr>
            <tr>
                <th>Borrar</th>
                <td>{{ include('gatoperro/_delete_form.html.twig') }}</td>
            </tr>
            <tr>
                <th>Editar</th>
                <td><a href="{{ path('gatoperro_edit', {'id': gatoperro.id}) }}"><i class="far fa-edit"></i></a></td>
            </tr>
        </tbody>
    </table>
    </div>
    </div>
    <a href="{{ path('gatoperro_index') }}"><i class="fas fa-arrow-alt-circle-left"></i>Volver a Lista</a>

    
</div>
    </div>
    </div>

{% endblock %}


   

