<form method="post" action="{{ path('categoria_delete', {'id': categorium.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorium.id) }}">
    <button class="non"><i class="fas fa-trash-alt bac"></i></button>
</form>
